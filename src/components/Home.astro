---
import illustration from '../assets/justin-dog-mountains.svg';
import paperBackground from '../assets/paper-background.jpeg';
import mandySong from '../assets/MandyConnell-Theme_COUNTRY.mp3';
---

<div class="container">
    <img id="background" src={paperBackground.src} alt="" fetchpriority="high" />

    <main>
      <div class="title">Colorado Justin</div>

      <ul class="links-collection">
        <li class="links contact-collab">
          <span>Contact / Collaborate: </span>
          <br />
          <a href="mailto:justin@coloradojustin.com">justin@coloradojustin.com</a>
        </li>
    
        <li class="links audio-player">
          <span>Mandy Connell Theme Song: </span>
          <br />
          <audio controls>
            <source src={mandySong} type="audio/mpeg" />
            Your browser does not support the audio element.
          </audio>
          <br />
          <a href={mandySong} download>Free Download</a>
        </li>
    
        <div class="links instagram">
          <span>Instagram: </span>
          <br />
          <a href="https://www.instagram.com/justinfromcolorado" target="_blank">@justinfromcolorado</a>
        </div>
      </ul>
  
      <div class="illustration">
        <img src={illustration.src} alt="Colorado Justin Illustration" />
      </div>
  </main>

  <footer>&copy; 2025 Justin Renaud. All rights reserved.</footer>
</div>

<style>
    :root {
      --blue: #0054ff;
      --red: #ec1845;
      --grey: #786c68;
      --white: #fff;
    }
    
    #background {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
      /* filter: blur(100px); */
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #f1e2c7;
      color: var(--grey);
      margin: 0;
      font-size: 1rem;
      padding: 0;
    }

    .container {
      text-align: center;
      padding: 2rem 0;
    }

    ul {
      list-style-type: none;
      padding: 0;
      margin-block-start: 0;
      margin-block-end: 0;
      padding-inline-start: 0;
    }

    .title {
      font-size: 56px;
      color: var(--red);
      font-weight: bold;
      text-transform: uppercase;
      margin-bottom: 1.5rem;
      position: relative;
      left: -100%;
      animation: slideIn 1.5s ease-out forwards;
    }

    @keyframes slideIn {
      from {
        left: -100%;
      }
      to {
        left: 0;
      }
    }

    .links-collection {
      display: flex;
      flex-direction: column;
      justify-content: space-evenly;
      gap: 1rem;
      line-height: 1.375;
    }

    a {
      color: var(--blue);
      text-decoration: none;
      margin: 0 1rem;
      padding: 0.2rem 0.5rem;
      background-color: transparent;
      transition: all 0.3s ease;
    }

    a:hover {
      background-color: var(--blue);
      color: var(--white);
      text-decoration: none;
    }

    /* mobile order */
    .contact-collab {
      order: 2;
    }
    .audio-player {
      order: 1;
    }
    .instagram {
      order: 3;
    }

    .illustration {
      margin-top: 2rem;
    }

    .illustration img {
      width: 208%;
      height: auto;
      max-width: 1280px;
      left: -50%; /* just past edge of guitar */
      position: relative;
    }

    footer {
      margin-top: 1rem;
      font-size: 0.9rem;
    }

    @media screen and (min-width: 1080px) {
      .title {
        font-size: 112px;
      }

      body {
        font-size: 1.5rem;
      }

      .links-collection {
        flex-direction: row;
        gap: 2rem;
        max-height: 100px;
        margin: 0 2rem;
        overflow: visible;
      }

      .contact-collab {
        order: 1;
      }
      .audio-player {
        order: 2;
      }
      .instagram {
        order: 3;
      }

      .illustration img {
        width: 100%;
        height: auto;
        max-width: 1280px;
        left: 0;
      }
    }
  </style>